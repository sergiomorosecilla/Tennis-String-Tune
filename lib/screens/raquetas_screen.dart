import 'package:flutter/material.dart';

class RaquetasScreen extends StatelessWidget {
  const RaquetasScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Raquetas')),
      body: const Center(child: Text('Módulo Raquetas — próximamente')),
    );
  }
}