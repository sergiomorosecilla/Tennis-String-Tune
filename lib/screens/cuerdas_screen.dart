import 'package:flutter/material.dart';

class CuerdasScreen extends StatelessWidget {
  const CuerdasScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Cuerdas')),
      body: const Center(child: Text('Módulo Cuerdas — próximamente')),
    );
  }
}